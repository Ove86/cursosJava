const material = "20 Pulgadas de hierro";

console.log(material);

// Cambia Pulgadas por "
console.log(material.replace('Pulgadas','"'));

// slice para cortar
console.log(material.slice(0,11));

// Quedarse solo con la primera letra
console.log(material.charAt(0));