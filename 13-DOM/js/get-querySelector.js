// seleccionar elemento por clase

const header = document.getElementsByClassName  ('header');
console.log (header);

// seleccionar elemento por id

const header2 = document.getElementById ('formulario');
console.log (header2);

// queryselector selecciona clases, id o elementos HTML (solo coge un elemento)
// queryselectorAll coge todos los elementos que cumplan una condicion

const card = document.querySelector('.card');
console.log (card);

